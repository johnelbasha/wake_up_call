source "https://rubygems.org"

# to grab secrets from the local .env file
gem "dotenv"

# to make HTTP requests
gem 'faraday'

# need to specify this to overcome conflicts with HTTParty gem
gem "json", "~> 2.7"

# to send emails
gem "resend", ">= 1.10.0", "< 1.13.0"